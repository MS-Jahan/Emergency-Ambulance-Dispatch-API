#!/bin/sh
# Drop Cursor/Claude attribution trailers before the commit is finalized.
msg_file="$1"
[ -f "$msg_file" ] || exit 0
grep -viE '^(Co-authored-by|Co-Authored-By|Claude-Session|Made-with):' "$msg_file" > "${msg_file}.tmp"
mv "${msg_file}.tmp" "$msg_file"
